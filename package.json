{
  "scripts": {
    "build": "prisma generate",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "prisma": "^5.0.0",
    "nodemailer": "^6.9.0",
    "twilio": "^4.0.0"
  }
}
